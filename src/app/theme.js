'use client'

import {createTheme} from "@mui/material";

export const theme = createTheme({
  palette: {
    theme: {
      main: "#fefefe",
      light: "#ededed",
      dark: "#0a0a0a",
      contrastText: "#fefefe",

    }
  }
}
);

export default theme;
